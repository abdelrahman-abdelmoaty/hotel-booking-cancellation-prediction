# MongoDB Atlas Connection Configuration
# 
# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace the placeholder values with your actual MongoDB Atlas credentials
# 3. Get your connection string from MongoDB Atlas dashboard:
#    - Go to your cluster → Connect → Connect your application
#    - Copy the connection string
#    - Replace <username> and <password> with your database user credentials
#
# Example connection string format:
# mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority

# MongoDB Connection String (REQUIRED)
# Format: mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority

# MongoDB Database Name (OPTIONAL - defaults used in code)
# The database name where your hotel bookings data will be stored
MONGODB_DB=hotel_bookings

# MongoDB Collection Name (OPTIONAL - defaults used in code)
# The collection name where your hotel bookings documents will be stored
MONGODB_COLLECTION=bookings
